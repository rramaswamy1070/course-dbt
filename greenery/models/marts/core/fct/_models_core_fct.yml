version: 2

models:
  - name: fct_orders
    description: User, order and product-level data (most granular)
    columns:
      - name: user_id
        tests:
          - not_null
      - name: order_id
        tests:
          - not_null